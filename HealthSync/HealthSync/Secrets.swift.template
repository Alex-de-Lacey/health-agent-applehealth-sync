// Copy this file to Secrets.swift and fill in your values.
// Secrets.swift is gitignored and must never be committed.
enum Secrets {
    static let apiURL = "http://YOUR_VPC_IP:3000"
    static let apiKey = "your-api-key-here"
}
